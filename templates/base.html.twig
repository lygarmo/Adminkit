<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin Dashboard{% endblock %}</title>
    
    <!-- AdminKit CSS por CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@adminkit/core@latest/dist/css/app.min.css">
</head>
<body>
    <div class="wrapper">
        <!-- Barra lateral -->
        <nav id="sidebar" class="sidebar js-sidebar">
            <div class="sidebar-content js-simplebar">
                <a class="sidebar-brand" href="/">
                    <span class="align-middle">Admin</span>
                </a>
                
                <ul class="sidebar-nav">
                    <li class="sidebar-item">
                        <a class="sidebar-link" href="{{ path('libro_index') }}">
                            <i class="align-middle" data-feather="book"></i> <span class="align-middle">Ver Libros</span>
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a class="sidebar-link" href="{{ path('autor_index') }}">
                            <i class="align-middle" data-feather="users"></i> <span class="align-middle">Ver Autores</span>
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a class="sidebar-link" href="{{ path('editor_index') }}">
                            <i class="align-middle" data-feather="edit"></i> <span class="align-middle">Ver Editores</span>
                        </a>
                    </li>
                </ul>
            </div>
        </nav>

        <div class="main">
            <main class="content">
                <div class="container-fluid p-0">
                    {% block body %}
                    <!-- Contenido principal de cada vista -->
                    {% endblock %}
                </div>
            </main>
        </div>
    </div>

    <!-- AdminKit JS por CDN -->
    <script src="https://cdn.jsdelivr.net/npm/@adminkit/core@latest/dist/js/app.min.js"></script>
    <script src="https://unpkg.com/feather-icons"></script>
    <script>feather.replace()</script>
</body>
</html>
