<form method="post" action="{{ path('app_libro_no_relacional_delete', {'id': libro_no_relacional.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ libro_no_relacional.id) }}">
    <button class="btn">Delete</button>
</form>
