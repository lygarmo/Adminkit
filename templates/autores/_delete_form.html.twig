<form method="post" action="{{ path('app_autores_delete', {'id': autore.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autore.id) }}">
    <button class="btn">Delete</button>
</form>
